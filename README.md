# HW6 — Cop & Thief: Dual AI Agents via MCP

Two autonomous AI agents — a **Cop** and a **Thief** — chase each other on a 5×5 grid under
**partial observability**, each running its own **MCP server** and communicating in natural
language. Assignment: Dr. Yoram Segal, "Dual AI Agent race via MCP".

> 🚧 Project scaffolding to come. This repo currently holds the shared match rules.

## Documents

- [`SHARED_MATCH_RULES.md`](SHARED_MATCH_RULES.md) — the common spec both teams build against for
  the inter‑group (bonus) match.

## Notes

- Secrets (`.env`, `credentials.json`, `token.json`, `*.key`, `*.pem`) are git‑ignored — never
  commit them.
